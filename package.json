{
  "name": "cautionlabs-prerendering",
  "version": "1.0.0",
  "description": "Prerendering worker for CautionLabs crawlers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler build"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/puppeteer": "^0.0.14",
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.78.0"
  }
}
